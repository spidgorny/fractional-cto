# fractional-cto

Vite + React landing page for a fractional CTO service.

## Requirements

- Node.js `^20.19.0` or `>=22.12.0`

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run lint`